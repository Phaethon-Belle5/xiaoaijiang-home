# Wrangler / Cloudflare 缓存
.wrangler/

# 依赖与构建产物
node_modules/
dist/
*.log

# 系统文件
.DS_Store
Thumbs.db

# 机密：一切密钥/凭据都不入库（部署时本地读取）
worker/.site_password
worker/.github_client_id
worker/.github_client_secret
worker/.netease_cookie
map-gallery-worker/.site_password
*.site_password
*.netease_cookie
*.github_client_secret

# Agent 本地缓存
.impeccable/

# 视频转码工作目录（视频本体在 R2，不入库）
videos-in/
videos-out/
video-urls.txt
